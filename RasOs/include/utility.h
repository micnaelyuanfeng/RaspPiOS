#ifndef _UTILITY_H_
#define _UTILITY_H_

/**
 *  assembler will but 1st argument into x0, 2nd argument into x2, and upto 4 arguments
 *  If there are more than 4 arguments, then use stack to pass arguments. 
**/
extern void _delay(uint64 wait_length);
extern void _put32(uint64 reg_addr, uint32 character);
extern void _

void put32();

#endif